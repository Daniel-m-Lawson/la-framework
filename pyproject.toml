[project]
name = "la-framework"
version = "0.1.0"
description = "Daniel's internal Python framework"
requires-python = ">=3.10"
dependencies = [
    "pyodbc>=4.0"
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]